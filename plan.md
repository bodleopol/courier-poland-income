1.  **Update `src/generate.js` for AdSense tags:**
    - Modify the `buildAdSenseScript` function to match the `<script>` tag logic from the screenshot.
    - Ensure both `<meta name="google-adsense-account" content="ca-pub-8323455138689324">` and the AdSense script tag `<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-8323455138689324" crossorigin="anonymous"></script>` are present.
    - Ensure that these tags are correctly injected into the `<head>` of all generated HTML pages (this is already handled by `sanitizeStaticHtmlHead` which injects `buildAdSenseScript`). Wait, the prompt says to make sure AdSense code is inserted into every page and the meta tag is also inserted and create the `Ads.txt` (or update it).

2.  **Verify `Ads.txt` generation:**
    - The `generate.js` script already generates `ads.txt` and `ada.txt`. Check if the format in the file generation matches what's required from the screenshot (`google.com, pub-8323455138689324, DIRECT, f08c47fec0942fa0`).
    - The code currently has:
      ```javascript
      const publisherId = 'ca-pub-8323455138689324';
      const pubOnly = publisherId.replace('ca-', '');
      const adsTxtLines = ['# ads.txt — rybezh.site'];
      adsTxtLines.push(`google.com, ${pubOnly}, DIRECT, f08c47fec0942fa0`);
      ```
      This evaluates to `google.com, pub-8323455138689324, DIRECT, f08c47fec0942fa0`, which perfectly matches the screenshot.
    - So `ads.txt` is actually already fine, but I'll ensure we review it.
    - I should check if `buildAdSenseScript` is matching the user screenshot. Let's look at `generate.js` line 1296:
      ```javascript
      function buildAdSenseScript() {
        // Use user-provided AdSense Publisher ID directly to ensure ads are loaded on all pages.
        const publisherId = 'ca-pub-8323455138689324';
        // Google AdSense Auto Ads — automatically finds the best ad placements on the page.
        return `<meta name="google-adsense-account" content="${publisherId}">
      <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${publisherId}" crossorigin="anonymous"></script>`;
      }
      ```
      Wait, the screenshot for AdSense script says:
      ```html
      <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-8323455138689324"
           crossorigin="anonymous"></script>
      ```
      The meta tag screenshot says:
      ```html
      <meta name="google-adsense-account" content="ca-pub-8323455138689324">
      ```
      These perfectly match what's currently generated by `buildAdSenseScript()`.

    Why did the user ask "Код AdSense на кожну сторінку встав, мата теги також і зроби файл Ads.txt" ?
    Maybe the code is NOT being inserted on every page?
    Let's check where `sanitizeStaticHtmlHead` is used.
