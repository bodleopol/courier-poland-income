# Environment Variables Example
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file with real values to git

# Google Site Verification Token (optional)
# Used for Google Search Console verification
GOOGLE_SITE_VERIFICATION_TOKEN=

# Indexable Vacancies Limit (optional, default: 50)
# Limits the number of vacancy pages that are indexable by search engines
INDEXABLE_VACANCIES_LIMIT=50
